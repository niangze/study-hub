<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studyhub.mapper.AnswerMapper">

    <resultMap id="AnswerVOMap" type="com.studyhub.vo.AnswerVO">
        <id column="id" property="id"/>
        <result column="content" property="content"/>
        <result column="question_id" property="questionId"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="user_avatar" property="userAvatar"/>
        <result column="is_accepted" property="isAccepted"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="selectAnswersByQuestionId" resultMap="AnswerVOMap">
        SELECT 
            a.*,
            u.username as username,
            u.avatar as user_avatar
        FROM answer a
        LEFT JOIN user u ON a.user_id = u.id
        WHERE a.question_id = #{questionId}
        ORDER BY a.is_accepted DESC, a.created_at DESC
    </select>

</mapper>
